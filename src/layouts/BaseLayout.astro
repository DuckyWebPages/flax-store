---
/* Base layout: inline header, anti-overlay hardening */
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title><slot name="title" />Flax Lignan Health</title>

    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Source+Sans+3:wght@400;600;700&display=swap" rel="stylesheet">

    <style>
      :root {
        --font-body: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
        --font-head: 'Source Sans 3', 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
        --ink: #0f172a; --ink-muted: #64748b; --bg: #f8fafc;
      }

      * { box-sizing: border-box; }
      body { margin: 0; font-family: var(--font-body); background: var(--bg); color: var(--ink); }

      /* ===== Fixed header that ALWAYS receives clicks ===== */
      .site-header-wrap {
        position: fixed; top: 0; left: 0; right: 0;
        z-index: 2147483647;
        background: #fff; border-bottom: 1px solid #e5e7eb;
        pointer-events: auto !important;
      }
      .site-header {
        max-width: 1100px; margin: 0 auto; padding: 10px 16px;
        display: flex; align-items: center; justify-content: space-between;
      }
      .brand { font-family: var(--font-head); font-weight: 800; letter-spacing: -0.01em; text-decoration: none; color: var(--ink); }
      nav.main-nav { display: flex; gap: 16px; align-items: center; }
      .nav-link {
        color: var(--ink); text-decoration: none; font-weight: 600; letter-spacing: .02em;
        padding: 6px 8px; border-radius: 8px;
        position: relative; z-index: 2; pointer-events: auto !important;
      }
      .nav-link:hover { background: #f3f4f6; }
      .nav-link[aria-current="page"] { text-decoration: underline; }

      /* Page shell (clear fixed header) */
      main, footer { max-width: 1100px; margin: 0 auto; }
      main { padding: 92px 16px 56px; }
      footer { padding: 16px; color: var(--ink-muted); }

      /* Headings */
      h1, h2, h3, h4, .brand-title, .products-title, .site-title {
        font-family: var(--font-head); line-height: 1.15; letter-spacing: -0.01em;
      }
      h1, .site-title, h2, .products-title { font-weight: 700; }
      h3, h4 { font-weight: 600; }

      /* ===== CLICKABILITY HARDENING ===== */
      /* Legacy hero overlays: never render them */
      .hero-actions, .hero-cta, .quick-actions { display: none !important; }

      /* Background layers must never catch clicks */
      .hero-bg, .hero::before, .hero::after, .overlay, .shade { pointer-events: none !important; }

      /* On home, a big hero can cover content — disable clicks on hero except the one CTA */
      .hero { pointer-events: none !important; }
      .hero * { pointer-events: none !important; }

      /* Re-enable clicks ONLY for the single Shop CTA under the tagline */
      .home-shop-cta, .home-shop-cta * { pointer-events: auto !important; position: relative; z-index: 5; }

      /* Bottom blog teaser: always clickable */
      .blog-update-wrap, .blog-update, .blog-update-wrap a { position: relative; z-index: 6; pointer-events: auto !important; }

      /* Belt-and-suspenders: every /blog link is clickable above all */
      a[href^="/blog"] { position: relative; z-index: 7; pointer-events: auto !important; }
    </style>
  </head>

  <body>
    <div class="site-header-wrap">
      <div class="site-header">
        <a href="/" class="brand">Flax Lignan Health</a>

        <nav class="main-nav" aria-label="Main">
          <a href="/" class="nav-link"
             aria-current={Astro.url.pathname === '/' ? 'page' : undefined}>
            Home
          </a>
          <a href="/products" class="nav-link"
             aria-current={Astro.url.pathname.startsWith('/products') ? 'page' : undefined}>
            Products
          </a>
          <a href="/blog/" class="nav-link"
             aria-current={Astro.url.pathname.startsWith('/blog') ? 'page' : undefined}>
            Blog
          </a>
          <a href="/contact" class="nav-link"
             aria-current={Astro.url.pathname === '/contact' ? 'page' : undefined}>
            Contact Us
          </a>
        </nav>
      </div>
    </div>

    <main>
      <slot />
    </main>

    <footer>
      © {new Date().getFullYear()} Flax Lignan Health
    </footer>
  </body>
</html>
