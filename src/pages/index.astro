---
const title = "Flax Lignan Health — Home";
---

<html lang="en">
  <head>
    <title>{title}</title>
  </head>
  <body>
    <!-- Hero Section -->
    <section class="hero">
      <h1 class="site-title">Flax Lignan Health</h1>
      <p>Nutrient-dense lignans and gentle detox support you can trust.</p>
    </section>

    <!-- Products Section -->
    <h2 id="products" class="products-title">Products</h2>

    <!-- Blog Update Example -->
    <div class="blog-update-wrap">
      <a href="/blog" class="blog-update">
        <span class="blog-icon">🌸</span>
        <span>Read our latest blog update</span>
      </a>
    </div>
  </body>
</html>

<style>
/* ---------- Site-wide body defaults ---------- */
html, body {
  font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  font-weight: 400;
  margin: 0;
}
p, li { line-height: 1.6; }

/* Smaller / subtle text */
small, .subtle, .muted, .fineprint {
  font-size: 0.9rem;
  font-weight: 300;
  opacity: 0.9;
}

/* ---------- Home hero ---------- */
h1.site-title {
  font-family: 'Source Sans 3', 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  font-size: clamp(2.2rem, 4.2vw, 3.2rem);
  font-weight: 700;
  letter-spacing: -0.01em;
  line-height: 1.1;
  color: black;
}

.hero p {
  font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  font-weight: 300;
  color: black;
}

/* ---------- Header / Nav ---------- */
.logo-text {
  font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  font-size: 1.2rem;
  font-weight: 400;
  letter-spacing: 0.01em;
  color: black;
  display: inline-block;
}

nav a {
  font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  font-size: 1.05rem;
  font-weight: 400;
  letter-spacing: 0.02em;
  text-transform: uppercase;
  text-decoration: none;
  color: inherit;
}
nav a:hover { opacity: 0.85; }

/* Hide logo text on small screens */
.logo-text { display: none; }
@media (min-width: 768px) {
  .logo-text {
    display: inline-block;
    margin-left: 0.25rem;
  }
}

/* ---------- Products page header ---------- */
.products-title {
  font-family: 'Source Sans 3', 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  font-size: 2rem;
  font-weight: 700;
  letter-spacing: -0.01em;
  margin-bottom: 1rem;
}

/* ---------- Products grid body copy (inside React island) ---------- */
.shop-app-root p,
.shop-app-root li,
.shop-app-root small,
.shop-app-root .desc,
.shop-app-root .fineprint,
.shop-app-root .muted {
  font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif !important;
  font-weight: 300;
  line-height: 1.6;
}

/* Front-page BLOG UPDATE link */
.blog-update {
  display: inline-flex;
  align-items: center;
  gap: 12px;
  font-family: 'Source Sans 3', 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  font-weight: 600;
  letter-spacing: 0.02em;
  font-size: clamp(1.3rem, 2.8vw, 1.8rem);
  line-height: 1;
  text-decoration: none;
}
.blog-update:hover { text-decoration: underline; opacity: .95; }

/* Blog update icon */
.blog-icon {
  width: 1.6em;
  height: 1.6em;
  flex: 0 0 auto;
  transform: translateY(0.02em);
}

/* Stacked version */
.blog-update--stacked {
  flex-direction: column;
  gap: 10px;
  text-align: center;
}
.blog-update--stacked .blog-icon {
  width: 2.2em;
  height: 2.2em;
}

/* Blog readability */
.blog-content p, .blog-content li { line-height: 1.7; font-size: 1.05rem; }
.blog-content h2, .blog-content h3 { margin-top: 1.25rem; }
.blog-content img { max-width: 100%; height: auto; border-radius: 10px; }

/* ===== FINAL OVERRIDES ===== */
.logo-text { display: none !important; }

header nav a,
header .nav a,
.site-header nav a,
nav.primary a,
nav a {
  font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif !important;
  font-weight: 400;
  letter-spacing: 0.02em;
  text-transform: uppercase;
  text-decoration: none;
  color: inherit;
}
</style>
